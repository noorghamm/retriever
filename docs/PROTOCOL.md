# Wire protocol

<!-- v1 spec goes here: document the protocol exactly as the baseline code
     implements it, before changing anything. Byte tables, one row per field.
     Sections below are prompts, not content — fill them in yourself. -->

## Version 1 (baseline)

### Request: LIST

### Request: GET

### Request: PUT

### Status codes

### Worked example — one full GET exchange, byte by byte

## Known problems with v1

<!-- e.g. status code 1 means three different failures -->
